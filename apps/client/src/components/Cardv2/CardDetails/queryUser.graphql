query GetUserCard($id: ID!) {
  user: getUser(id: $id) {
    name
    email
    projects {
      id
      title
      preview
      description
      createdAt
      siteLink
      repoLink
      isApproved
    }
  }
}
