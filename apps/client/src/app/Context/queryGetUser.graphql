query GetUserContext($id: ID!) {
  user: getUser(id: $id) {
    id
    name
    email
    projects {
      id
      title
      preview
      createdAt
      description
      siteLink
      repoLink
      isApproved
    }
  }
}
