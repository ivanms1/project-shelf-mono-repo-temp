fragment ProjectsResponseFragment on ProjectsResponse {
  nextCursor
  prevCursor
  totalCount
  results {
    id
    title
    createdAt
    author {
      name
      email
    }
    likes {
      id
    }
    preview
    repoLink
    siteLink
    description
    isApproved
  }
}
